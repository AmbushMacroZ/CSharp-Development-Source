RURU:
Для компиляции exe-файла/ов необходимо:
 - В папке 'src' создать например файл 'program.cs' а в него записать все ваши данные для exec файла.
 - После заполнения файла(-ов) в папке 'scr' сохраните файл.
 - Для компиляции отредактируйте '_comile.cmd':
  - Измените значениe под себя:
    - '# File to compile path';
 - Запустите '_compile.cmd'.
 - Скомпилированый файл будет находится по пути '/build/fileName.exe'
